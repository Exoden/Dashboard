{% if stuff.type.name == "Weapon" %}
    Damages: {% if stuff is not null %}{{ stuff.characteristics.damageMinimum }} - {{ stuff.characteristics.damageMaximum }}{% else %}{{ hero.characteristics.damageMinimum }} - {{ hero.characteristics.damageMaximum }}{% endif %}<br />
    Precision: {% if stuff is not null %}{{ stuff.characteristics.hitPrecision }}{% else %}{{ hero.characteristics.hitPrecision }}{% endif %}<br />
    Critical: {% if stuff is not null %}{{ stuff.characteristics.criticalChance }}{% else %}{{ hero.characteristics.criticalChance }}{% endif %}%<br />
    Delay: {% if stuff is not null %}{{ stuff.characteristics.attackDelay }}{% else %}{{ hero.characteristics.attackDelay }}{% endif %} sec<br />
{% elseif stuff is not null and stuff.item.typeItem.name == "Stuff" %}
    Health: {% if stuff is not null %}{{ stuff.characteristics.health }}{% else %}0{% endif %}<br />
    Armor: {% if stuff is not null %}{{ stuff.characteristics.armor }}{% else %}0{% endif %}<br />
    Dodge: {% if stuff is not null %}{{ stuff.characteristics.dodge }}{% else %}0{% endif %}%<br />
    Blocking: {% if stuff is not null %}{{ stuff.characteristics.blocking }}{% else %}0{% endif %}%<br />
{% endif %}